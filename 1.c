/*
    I'm level 1 test.
*/
void main()
{
    int a, s = 2, b;
    a = 10; s = 0;
    a = 012, s = 0x34, b = 9;
    char ch, chh = 'b', chhh = 'c';
    scanf("%d", &ch);
    while(a > 0 && a <= 10 || a % 100 == 10 && !a == 10)
    {
        a -= 1;
        int a;
        a = 10;
        s += a;
        if(-s < -10)
        {
            printf("result is: %d\n", s);
            int b;
            b = 10;
            for(i=0; i < b; i++)
            {
                int aa;
                printf("Have fun: %d\n", i);
                for(int j; j < b; j++)
                {
                    int bb;
                }
                int vv;
                for(int asdasd; asd < 0; k++)
                {
                    int yy;
                }
            }
        }
    }
}
// No more compilation error.
